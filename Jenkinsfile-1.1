pipeline {
  agent {
    docker { image 'node:16-alpine' }
  }
  stages {
    stage('Test') {
      steps {
        sh 'node --version'
      }
    }
   stage('update yum') {
     agent {
     docker { image 'bitnami/nginx:latest'}
          }
     steps {
       sh 'yum update'
      }
    }
  }
}
